== TODO
1. New menu duplication in FILE enu
2. Grep tool from Edit menu
3. In tools menu, hide AI assistant, AI console, Set API key
4. Add "Open dired" in dired menu
5. when one of terminals selected from terminal, in buffers menu mark does not updated.
   tab should be integrated? integrate them and add "buffer or terminal or app" after label in Buffers menu.
6. Text editor's dialogue's extenstion can be configurable in config setting menu. "Text Editor Default Extention" like *.txt;*.md;*.json
7. Hide AI tab in settings.
8. Hide toggle "AI(experimental)" in settings
9. when saved using "save as" menu, show the full path in window title
