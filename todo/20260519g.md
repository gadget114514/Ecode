feature: file menu: open, save as, use "*.txt;*.md;*.json" the three extentions for defaut.
feature: i want to create fastdired  ; the looks is same as dired but it uses no common control. it use direct2d to render ; multi thread read directory and files; gradually read the directory and display the info at early stage of reading directory;
