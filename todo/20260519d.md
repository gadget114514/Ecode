
1. Verify: In dired, center bar should be configurable and save/resume
2. Invesitigate: all  parameters should be whether save/resume or not;
                 some parameters should be whether set in setting or not;
3. Feature: set images/appicon.ico as app icon
4. Feature: in dired, add "Open with Windows shell" in context menu
5. add feature: in dired, top text field show the folder path; then drag and drop to the text field, then open the folder ; add right context menu at the text field to open the folder with windows shell.